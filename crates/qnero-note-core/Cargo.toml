[package]
name = "qnero-note-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Qnero note primitives that carry no lattice dependency: digests, domain tags, note commitments, nullifiers and the spend credential"

[dependencies]
qp-poseidon-core = { workspace = true }
rand_core = { workspace = true }
thiserror = { workspace = true }
hex = { workspace = true }
